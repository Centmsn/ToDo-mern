import styled from "styled-components";
import { useState } from "react";

import SideBar from "../SideBar";

const AddNote = ({ isOpen }) => {
  return <SideBar isOpen={isOpen}>Dupa</SideBar>;
};

const Wrapper = styled.div``;

export default AddNote;
